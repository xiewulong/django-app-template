from django.urls import path

from . import views


app_name = '{{ app_name }}'
urlpatterns = [
    path('', views.index, name='index'),
    path('polls/', views.PollsView.as_view(), name='polls'),
    path('polls/<int:id>', views.PollView.as_view(), name='poll'),
]
